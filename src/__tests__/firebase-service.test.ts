/**
 * @jest-enviorment-node
 */

import { querySchedule, addMatch, modifyMatch, removeMatch, removeOldMatches } from '../utils/firebase-service';

describe('querySchedule', () => {
    
});

describe('addMatch', () => {
    
});

describe('modifyMatch', () => {
    
});

describe('removeMatch', () => {
    
});

describe('removeOldMatches', () => {
    
});